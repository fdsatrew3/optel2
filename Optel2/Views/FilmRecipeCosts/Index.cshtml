@model IPagedList<Optel2.Models.FilmRecipeCost>

@{
	ViewBag.Title = "Film recipe costs data";
}

@using X.PagedList.Mvc;
@using X.PagedList;
@using X.PagedList.Mvc.Common;
@using System.Web.Configuration;
@using Optel2.Models;

<div class="row">
	<div class="col-auto">
		<a href="@Url.Action("Create")" class="btn btn-dark left-menu-btn"><div class="float-left"><img src="~/Images/Menu/add.png" width="24px" height="24px">&nbsp;Create new film recipe cost data</div></a>
	</div>
</div>
<table class="custom-table desktop-ver">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.First().Recipe)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.First().Thickness)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.First().Cost)
			</th>
			<th></th>
		</tr>
	</thead>
	@foreach (FilmRecipeCost item in ViewBag.PageContent)
	{
		<tr>
			<td>
				@Html.DisplayFor(modelItem => item.Recipe)
			</td>
			<td>
				@Html.DisplayFor(modelItem => item.Thickness)
			</td>
			<td>
				@Html.DisplayFor(modelItem => item.Cost)
			</td>
			<td>
				<a href="@Url.Action("Edit", new { id=item.Id })" title="Edit"><img src="/Images/Menu/edit.png" width="24px" height="24px"></a>
				<a href="@Url.Action("Details", new { id=item.Id })" title="Details"><img src="~/Images/Menu/info.png" width="24px" height="24px"></a>
				<a href="@Url.Action("Delete", new { id=item.Id })" title="Delete"><img src="~/Images/Menu/delete.png" width="24px" height="24px"></a>
			</td>
		</tr>
	}
</table>
<div class="d-flex flex-row flex-wrap justify-content-center mobile-ver-flex">
	@foreach (FilmRecipeCost item in ViewBag.PageContent)
	{
		<div class="p-1" style="width: 192px;">
			<table class="custom-table-horizontal">
				<tr>
					<td style="vertical-align: middle;" colspan="2">
						<a href="@Url.Action("Edit", new { id=item.Id })" title="Edit"><img src="/Images/Menu/edit.png" width="24px" height="24px"></a>
						<a href="@Url.Action("Details", new { id=item.Id })" title="Details"><img src="~/Images/Menu/info.png" width="24px" height="24px"></a>
						<a href="@Url.Action("Delete", new { id=item.Id })" title="Delete"><img src="~/Images/Menu/delete.png" width="24px" height="24px"></a>
					</td>
				</tr>
				<tr>
					<th>&nbsp;&nbsp;@Html.DisplayNameFor(model => item.Recipe)&nbsp;&nbsp;</th>
					<td>&nbsp;&nbsp;@Html.DisplayFor(model => item.Recipe)&nbsp;&nbsp;</td>
				</tr>
				<tr>
					<th>&nbsp;&nbsp;@Html.DisplayNameFor(model => item.Thickness)&nbsp;&nbsp;</th>
					<td>&nbsp;&nbsp;@Html.DisplayFor(model => item.Thickness)&nbsp;&nbsp;</td>
				</tr>
			</table>
		</div>
	}
</div>
<br />
@Html.PagedListPager((IPagedList)ViewBag.PageContent, page => Url.Action("Index", new { page = page }),
	new PagedListRenderOptions
	{
		LiElementClasses = new string[] { "page-item" },
		PageClasses = new string[] { "page-link" },
		MaximumPageNumbersToDisplay = Convert.ToInt32(WebConfigurationManager.AppSettings["MaxPagesInIndexPaginator"]),
		DisplayLinkToFirstPage = PagedListDisplayMode.Never,
		DisplayLinkToLastPage = PagedListDisplayMode.Never,
		DisplayLinkToPreviousPage = PagedListDisplayMode.Always,
		DisplayLinkToNextPage = PagedListDisplayMode.Always,
		DisplayEllipsesWhenNotShowingAllPageNumbers = false
	})


